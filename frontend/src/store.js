// Dependencies
import { createStore } from 'vuex';

//Store configuration
const mutations = {};

const state = {};

const store = createStore({ mutations, state, strict: true });

export default store;
